// app/(tabs)/profile.tsx
// TEMPORARY: OpenRouter Test Component
// Original profile code is saved in git history (commit e4e73e6)
// To restore: git checkout HEAD~1 -- app/(tabs)/profile.tsx

import OpenRouterTest from '@/components/OpenRouterTest';

export default function ProfileScreen() {
  return <OpenRouterTest />;
}
